/* generated by xckernel; do not edit. */
#include "xckernel/kernels/xck_lda_r_o1.hpp"

extern "C" {

const char* xck_lda_r_o1_scal_names[2] = {
    "w",
    "vrho",
};
extern const int xck_lda_r_o1_n_scal;
const int xck_lda_r_o1_n_scal = 2;
extern const int xck_lda_r_o1_n_fields;
const int xck_lda_r_o1_n_fields = 1;

/* The C ABI keeps one homogeneous scal list (all double);
 * fields come first, functional-derivative arrays last, split at
 * xck_lda_r_o1_n_fields. */
int xck_lda_r_o1(int64_t npts, int64_t nbf,
           const double* chi, const double* dchi,
           const double* lapl_chi, const double* hess_chi,
           const double* const* scal, double* out) {
    return xckernel::xck_lda_r_o1_t<double, double>(
        npts, nbf, chi, dchi, lapl_chi, hess_chi,
        scal, scal + 1, out);
}

} // extern C
