/* machine-generated by xckernel; do not edit. Copyright (c) 2026 Susi Lehtola. */
#pragma once
#include <cstdint>
#include "xckernel/evaluator.hpp"

namespace xckernel {

/* Exc = sum_g w_g rho_g zk_g (zk = Libxc energy density per particle).
 * T: host precision; Txc: the Libxc array type (double from Libxc). */
template <typename T, typename Txc = T>
XCK_HD inline T xck_mgga_tau_r_o0_t(int64_t npts, const T* w, const T* rho,
                         const Txc* zk) {
    T acc = T(0);
    for (int64_t g = 0; g < npts; ++g) acc += w[g] * rho[g] * T(zk[g]);
    return acc;
}

} // namespace xckernel
