# Static convenience lib; ships inside the librgpot umbrella (link_whole).
morse = static_library(
    'morse',
    'MorsePot.cc',
    dependencies: _deps,
    cpp_args: _args,
    include_directories: ['../../'],
    pic: true,
    install: false,
)
