[package]
name = "potctl"
version = "0.1.0"
edition = "2021"
description = "rgpot project control plane: release lockstep and related repo tasks"
license = "MIT"
repository = "https://github.com/OmniPotentRPC/rgpot"
publish = false

[[bin]]
name = "potctl"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive", "env"] }
