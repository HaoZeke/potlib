The Cap'n Proto schema is pinned to canonical [potentials-schema](https://github.com/OmniPotentRPC/potentials-schema) v1.5.0: `MetatomicParams` arm (union ordinal 4) with the upstream requested-outputs surface, typed NWChem parity (dplot/esp) and typed CPMD prop/linres/pimd/path/tddft blocks, `CommonMethodSpec` overlay, and a schema-sync CI gate that fails when the vendored copies diverge from the pinned release.
